ARTICLES
    {% for article in articles %}
    * {{article.getTitle()}}
    * {{article.getContent()}}
    {% endfor %}

CATEGORIES
    {% for category in categories %}
    * {{category.getTitle()}}
    {% endfor %}